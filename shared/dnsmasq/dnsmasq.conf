# Configurazione dnsmasq per domini .test
# Installa dnsmasq: brew install dnsmasq (su macOS)

# Risolvi tutti i domini *.test a localhost
address=/.test/127.0.0.1

# Opzionale: imposta il server DNS upstream
# server=8.8.8.8
# server=8.8.4.4
