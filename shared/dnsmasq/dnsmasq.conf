# dnsmasq configuration for .test domains
# Install dnsmasq: brew install dnsmasq (on macOS)

# Resolve all *.test domains to localhost
address=/.test/127.0.0.1

# Optional: set upstream DNS server
# server=8.8.8.8
# server=8.8.4.4
